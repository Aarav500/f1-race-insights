Rules:
- Do not delete files unless explicitly asked.
- Do not invent results, metrics, or data.
- Keep modeling code inside f1/models.
- Baselines must exist before custom models.
- Time-based splits only (no data leakage).
- Update README.md when endpoints change.
